import 'package:flutter/material.dart';
import 'textstyle_constants.dart';

const kApplyCouponText = Text(
  "Apply Coupon",
  style: kApplyCouponStyle,
);

const kEnterOTPtext = Text(
  "Enter OTP",
  style: kEnterOTPstyle,
);

const kCheckMessageBoxText = Text(
  "Check your message box.",
  style: kCheckMessageBoxStyle,
);
const kDontReceiveCodeText = TextSpan(
  text: "If you don't receive a code",
  style: kDontReceiveCodeStyle,
);

const kResendText = TextSpan(
  text: " Resend",
  style: kResendStyle,
);

const kConfirmOTPtext = Text("Confirm OTP");
const kPlatesInCartText = TextSpan(
  text: "Plates in cart ",
  style: kPlatesInCartStyle,
);
const kMyOrderButtonText = Text("My Order", style: kMyOrderButtonStyle);
const kIntroText = TextSpan(
  text:
      "Booking food is made easy throught three easy steps. Browse, Order and Track.\n",
  style: kIntroStyle,
);
const kEatGoodText = TextSpan(
  text: "Eat good and eat exciting",
  style: kEatGoodStyle,
);
const kBackText = Text(
  "Back",
  style: kBackStyle,
);

const kFreshMenuText = Text(
  "Fresh Menu",
  style: kFreshMenuStyle,
);
const kNoDataText = Text(
  "NO DATA",
  style: kNoDataStyle,
);
const kPerPlatePriceText = Text(
  "Per Plate \$30",
  style: kPerPlatePriceStyle,
);

const kAddressText = Text(
  "2142 Dogwood Ave Spooner Street, Bahamas.",
  style: kAddressTextStyle,
);
const kOfficeText = Text(
  "Office",
  style: kOfficeStyle,
);
const kDeliveryAddressText =
    Text("Delivery Address", style: kDeliveryAddressStyle);
const kTaxChargesText = Text(
  "Tax \& Charges",
  style: kPerPlatePriceStyle,
);

const kDeliveryFeeText = Text(
  "Delivery fee",
  style: kPerPlatePriceStyle,
);
const kItemTotalText = Text(
  "Item Total",
  style: kPerPlatePriceStyle,
);

const kToPayText = Text(
  "To Pay",
  style: kToPayStyle,
);

const kTaxCharges01Text = Text(
  "\$01",
  style: kPerPlatePriceStyle,
);
const kDeliveryFee01Text = Text(
  "\$01",
  style: kPerPlatePriceStyle,
);
